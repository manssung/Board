import React from "react";
import ReactDOM from "react-dom/client";
import StrategyGenerator from "./StrategyGenerator";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<StrategyGenerator />);